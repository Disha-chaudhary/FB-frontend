var x=10;
function add(){
    console.log(x);
    x++;
}
add();
add();
add();
//pure functions are those which given same input will always return same output and does not have any side effects.
//impure functions are those which given same input may return different output and may have side effects.